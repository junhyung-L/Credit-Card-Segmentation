# 신용카드 고객 세그먼트 분류: 결측치부터 다시 설계한 대규모 정형 데이터 실험

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 프로젝트 개요

신용카드 고객을 여러 세그먼트로 분류하는 대회형 분석 프로젝트다. 단순히 모델을 하나 고르는 문제가 아니었다. 월별 고객·신용·매출·청구·잔액·채널·마케팅 데이터를 합치면 변수는 857개에 이르고, 컬럼마다 결측이 생긴 이유도 달랐다. 보관된 실험 보고서의 분석 표본은 약 24만 건이다. 이 환경에서 중요한 판단은 “결측치를 무엇으로 채울까”보다, 어떤 결측을 정보로 남기고 어떤 결측만 보정할지 먼저 정하는 일이었다.

## 문제를 푼 방식

첫 단계에서는 월별 원천 테이블을 월과 고객 ID 기준으로 결합했다. 이후 결측률을 일괄 처리하지 않고 세 구간으로 나눴다. 결측률이 50% 이상인 컬럼은 제거 후보로 두고, 20~50% 구간은 예측 기반 대체를 검토했으며, 그보다 낮은 범주는 `Unknown` 처리처럼 모델이 결측 자체를 인식할 수 있는 방식을 사용했다. `missing_mechanism_analysis.py`에는 결측률을 계산하는 흐름이, `preprocess_missing_features.py`에는 휴대 가능한 기본 전처리 경로가 남아 있다.

모델링도 처음부터 하나로 고정하지 않았다. XGBoost와 CatBoost 같은 트리 모델을 기준선으로 두고, 로지스틱 회귀·랜덤 포레스트·DNN/MLP/CNN·TabNet을 차례로 비교했다. 정형 데이터에서 복잡한 모델이 항상 이기지 않는다는 점을 확인하기 위해서다. 특히 범주형 변수와 결측치를 다루는 CatBoost를 강한 단일 모델로 삼고, 서로 다른 오류를 낼 가능성이 있는 CatBoost·Logistic Regression·MLP를 meta-Logistic Regression으로 묶은 스태킹까지 실험했다.

## 핵심 판단과 결과

20,000건 검증 표본에서 CatBoost 단일 모델은 weighted F1 0.8893을 기록했다. 스태킹은 0.8936으로 조금 더 높았다. 수치 차이는 크지 않지만, 이 결과는 모델을 늘리는 것보다 결측 처리와 검증 조건을 먼저 맞추는 일이 중요하다는 판단을 뒷받침했다. TabNet도 0.8285를 기록했지만, 이 데이터에서는 트리 기반 접근보다 우위라고 보기 어려웠다.

대회 결과로는 public score 0.64636(75위), private score 0.6251(58위·상위 25%)가 실험 요약에 남아 있다. 이 점수와 2만 건 검증 F1은 같은 수치가 아니다. 전자는 대회 평가 결과이고, 후자는 별도의 표본 검증 결과다. 두 수치를 섞지 않고 함께 기록한 이유는, 내부 검증에서 좋아 보인 모델이 실제 대회 평가에서도 같은 수준으로 재현된다고 단정하지 않기 위해서다.

![보관된 실험의 F1 비교](images/model_f1_comparison.png)

*그림 1. 보관된 2만 건 표본 실험의 모델별 F1 비교. 스태킹의 0.8936은 전체 원본 데이터 재실행 결과가 아니라 해당 검증 조건에서의 기록이다.*

## 이 프로젝트가 보여주는 역량

이 작업의 중심은 모델 이름을 많이 나열하는 데 있지 않다. 서로 성격이 다른 월별 테이블을 합치고, 결측이 많은 변수를 무작정 평균으로 채우지 않으며, 단일 모델과 앙상블을 같은 기준에서 비교한 과정에 있다. 성능이 높은 모델 하나를 고른 뒤에도 그 점수가 어떤 데이터와 검증 조건에서 나온 것인지 구분한 점이 이 프로젝트의 실무적 가치다.

## 한계와 다음 단계

원본 데이터, 제출 파일, 공식 리더보드 export는 저장소에 포함되어 있지 않다. 따라서 대회 점수는 보관된 `reports/실험요약.md`의 기록으로 제시하며, 독립적으로 다시 실행한 결과라고 주장하지 않는다. 다음 단계는 원본 스키마와 데이터 버전을 명시하고, 분할별 클래스 비율·per-class F1·혼동행렬을 같은 실험 매니페스트에 남기는 것이다. 그래야 weighted F1이 가릴 수 있는 소수 세그먼트의 오류까지 판단할 수 있다.

## 근거

- 실험 요약: [`reports/실험요약.md`](reports/실험요약.md)
- 결측 분석: [`src/missing_mechanism_analysis.py`](src/missing_mechanism_analysis.py)
- 전처리 기본 경로: [`src/preprocess_missing_features.py`](src/preprocess_missing_features.py)
- 프로젝트 리뷰: [`docs/PROJECT_REVIEW.md`](docs/PROJECT_REVIEW.md)
